<Window x:Class="ClockbusterApps.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Clockbuster Apps" Height="350" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">

    <DockPanel>
        <Menu DockPanel.Dock="Top" Background="White" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1">
            <MenuItem Header="File">
                <MenuItem Header="Exit" Click="Exit_Click" />
            </MenuItem>
            <MenuItem Header="View">
                <MenuItem Header="Application Data" Click="ViewData_Click" />
            </MenuItem>
        </Menu>

        <StatusBar DockPanel.Dock="Bottom" Background="#F5F5F5" Height="28">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Status: " FontWeight="SemiBold" FontSize="12"/>
                    <TextBlock x:Name="StatusText" Text="Idle" FontSize="12"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>

        <Grid Margin="20">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Application Monitor" 
                           FontSize="24" FontWeight="Bold" 
                           Margin="0,0,0,10" 
                           HorizontalAlignment="Center"
                           Foreground="#2C3E50"/>

                <TextBlock Text="Track which applications you use and for how long" 
                           FontSize="13" 
                           Margin="0,0,0,30" 
                           HorizontalAlignment="Center"
                           Foreground="#7F8C8D"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           MaxWidth="350"/>

                <Button x:Name="BtnToggleLogging" 
                        Content="Start Monitoring" 
                        Width="180" Height="55"
                        Click="BtnToggleLogging_Click"
                        Background="#27AE60" 
                        Foreground="White" 
                        FontSize="16" 
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        BorderThickness="0"
                        Margin="0,0,0,20">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="View Application Data" 
                        Width="180" Height="40"
                        Click="ViewData_Click"
                        Background="#3498DB" 
                        Foreground="White" 
                        FontSize="14"
                        Cursor="Hand"
                        BorderThickness="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>